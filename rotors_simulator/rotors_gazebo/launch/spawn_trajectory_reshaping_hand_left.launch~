<?xml version="1.0"?>

<launch>
  <arg name="project_name" default="trajectory_reshaping"/>
  <arg name="namespace" default="$(arg project_name)_realistic_hand_left"/>
  <arg name="model" default="$(find trajectory_reshaping_world_visualizer)/res/realistic_hand/realistic_hand_left.xacro"/>
  <arg name="tf_prefix" default="$(arg namespace)"/>
  <arg name="x" default="0.0"/>
  <arg name="y" default="0.5"/>
  <arg name="z" default="1.0"/>

  <!-- send the robot XML to param server -->
  <param name="robot_description" command="$(find xacro)/xacro.py '$(arg model)' namespace:=$(arg namespace)"/>
  <param name="tf_prefix" type="string" value="$(arg tf_prefix)" />

  <!-- push robot_description to factory and spawn robot in gazebo -->
  <node name="spawn_$(arg namespace)" pkg="gazebo_ros" type="spawn_model" args="-param robot_description -urdf -x $(arg x) -y $(arg y) -z $(arg z) -model $(arg namespace)" respawn="false" output="screen">
  </node>
</launch>
