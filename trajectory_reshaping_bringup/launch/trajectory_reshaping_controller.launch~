<?xml version="1.0"?>

<launch>
  <arg name="namespace_global" default="/reshaping"/>
  <arg name="namespace" default="/reshaping/bebop2camera"/>
  <arg name="mav_name" default="bebop2camera"/>
  <arg name="model" default="$(find rotors_description)/urdf/bebop2camera_model_controller.urdf"/>
  <arg name="tf_prefix" default="$(arg mav_name)"/>

  <group ns="$(arg namespace)">

  <!-- send the robot XML to param server -->
  <!--<param name="robot_description" command="
    $(find xacro)/xacro.py '$(arg model)'
    enable_logging:=$(arg enable_logging)
    enable_ground_truth:=$(arg enable_ground_truth)
    enable_mavlink_interface:=$(arg enable_mavlink_interface)
    log_file:=$(arg log_file)
    wait_to_record_bag:=$(arg wait_to_record_bag)
    mav_name:=$(arg mav_name)
    namespace:=$(arg namespace)
    prefix:=$(arg mav_name)"
  />-->
  <param name="bebop2camera_description" command="$(find xacro)/xacro.py '$(arg model)' prefix:=$(arg mav_name)/"/>

  <param name="tf_prefix" type="string" value="$(arg tf_prefix)" />

  <!-- push robot_description to factory and spawn robot in gazebo -->
  <!--<node name="spawn_$(arg mav_name)" pkg="gazebo_ros" type="spawn_model" args="-gazebo_namespace $(arg namespace_global)/gazebo -param robot_description -urdf -x $(arg x) -y $(arg y) -z $(arg z) -model $(arg mav_name)" respawn="false" output="screen"></node>-->

  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false" output="screen" args="--namespace=$(arg namespace) joint_state_controller cam_yaw_joint_position_controller cam_pitch_joint_position_controller">
	<rosparam file="$(find rotors_control)/config/bebop2camera_control.yaml" command="load" ns="$(arg namespace_global)"/>
  </node>
  </group>

</launch>
